/**
 * Published contact details and the content policy shown inside the app.
 *
 * App Store Guideline 1.2 requires a UGC app to publish a way to reach the
 * people who run it, reachable from inside the app itself — not only from a
 * website. Both values below are placeholders until the team owns a real
 * mailbox and a hosted policy page; change them here and every surface follows.
 *
 * TODO(before TestFlight): replace SUPPORT_EMAIL with a mailbox that is
 * actually monitored, and set CONTENT_POLICY_URL once the page is hosted. The
 * in-app policy text below stands on its own if the URL stays null.
 */
export const SUPPORT_EMAIL = "support@iliapulse.gr";

export const CONTENT_POLICY_URL: string | null = null;

/** Target Apple expects us to state and meet for acting on a report. */
export const REPORT_RESPONSE_HOURS = 24;
