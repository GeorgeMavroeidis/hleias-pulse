# Braindump — Mavroeidis

Raw thinking. Nothing here is a commitment. What survives gets proposed to
[ARCHITECTURE.md](../ARCHITECTURE.md) by PR.

Newest first. Greek or English, whichever the thought arrives in.
Struck-through entries were wrong — they stay, because the wrong turns are worth as much
as the right ones.

> Margaris opened this file so the convention exists on both sides. Nothing in it is his
> — it is yours to fill. He will not edit it; if he needs to respond to something here he
> does it in his own file or in the PR.

---

## Open, from Margaris — 2026-09-05

Three things landed in his braindump that need your side of the argument before they can
move to `ARCHITECTURE.md`:

1. **Contracts before implementations** (§3). Publish the TypeScript signatures as a small
   PR that throws, then implement. Costs you one extra PR per feature; buys him the
   ability to start the same day. First candidate is MOD-1.
2. **Web beta leads, Apple deferred to 5 January 2027** (§3). No capital available now.
   Changes Stage 1's exit criterion from TestFlight to web beta. Wants your read on
   whether the Capacitor pipeline can still be built and Simulator-tested meanwhile.
3. **`docs/` ownership.** It falls to you under the `*` default in CODEOWNERS. If this
   folder is going to work as a shared surface, it probably wants explicit lines:
   `docs/braindump/margaris.md` to him, `docs/braindump/mavroeidis.md` to you,
   `docs/ARCHITECTURE.md` and `docs/README.md` to both. **He has not touched
   `.github/CODEOWNERS` — that is your file.**

---

## Entries

- *(empty — yours to write)*
