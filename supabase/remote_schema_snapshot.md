# Remote Public Schema Snapshot

Captured: 2026-06-14

Project ref: `uihwsndveblfgmlhdngi`

The linked Supabase project's `public` schema was inspected through a direct Postgres metadata query because Docker was not running locally and `supabase db pull` / `supabase db dump` require Docker.

Result:

- Public tables: none
- Public RLS policies: none

No existing public application tables need to be preserved before the ΗΛΕΙΑ PULSE schema migration.

